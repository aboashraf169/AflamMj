import Foundation

// Mock data removed — the app now uses live data from The Movie Database (TMDB).
// All content is fetched via TMDBContentRepository → TMDBClient.
// API key is configured in TMDBClient.swift (TMDBConfig.apiKey).
